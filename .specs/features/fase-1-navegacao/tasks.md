# Fase 1 — Estrutura de Navegação e Contexto Tasks

## Execution Protocol (MANDATORY -- do not skip)

Implement these tasks with the `tlc-spec-driven` skill: **activate it by name and follow its Execute flow and Critical Rules.** Do not search for skill files by filesystem path. The skill is the source of truth for the full flow (per-task cycle, sub-agent delegation, adequacy review, Verifier, discrimination sensor).

**If the skill cannot be activated, STOP and tell the user — do not proceed without it.**

---

**Design**: `.specs/features/fase-1-navegacao/design.md`
**Status**: Draft

---

<!-- The three sections below are generated by step 1.5 of the Tasks process. They are provisional — confirmed by user during task approval. -->

## Test Coverage Matrix

> **⚠️ PROVISIONAL — needs user confirmation.** No test files found in the codebase (`__tests__/` empty; no `*.test.*` or `*.spec.*` files). AGENTS.md mandates "Unit tests (Jest) + security best practices" and `@testing-library/react-native` is installed. Below is inferred from AGENTS.md + strong defaults (tlc-spec-driven template). Confirm or adjust before Execute.

| Code Layer | Required Test Type | Coverage Expectation | Location Pattern | Run Command |
| ---------- | ------------------ | -------------------- | ---------------- | ----------- |
| Screen component | unit | Render + text content per spec ACs (NAV-05–09) | `app/(tabs)/*.test.tsx` | `npm run test` |
| Tab layout | unit | Render all 4 tabs, active highlight, i18n labels (NAV-01–04) | `app/(tabs)/_layout.test.tsx` | `npm run test` |
| Root layout | unit | Stack renders, safe area, gesture handler (NAV-E01) | `app/_layout.test.tsx` | `npm run test` |
| i18n config | none | — (build gate only) | — | `npm run lint && npm run typecheck` |

## Parallelism Assessment

> Provisional — no test files exist to sample. Inferred from tooling: Jest 29 + `jest-expo` (single-file isolation by default). Confirm or adjust before Execute.

| Test Type | Parallel-Safe? | Isolation Model | Evidence |
| --------- | -------------- | --------------- | -------- |
| unit | Yes | Each test file is isolated; no shared mutable state | Jest default + no database/external store in this phase |

## Gate Check Commands

> Provisional — confirmed from `package.json` scripts. Confirm or adjust before Execute.

| Gate Level | When to Use | Command |
| ---------- | ----------- | ------- |
| Quick | After tasks with unit tests only | `npm run test` |
| Build | After phase completion or config-only tasks | `npm run lint && npm run typecheck` |
| Full | After tasks with e2e/integration | N/A (no e2e/integration in this phase) |

---

## Execution Plan

### Phase 1: Foundation (Sequential)

Tasks that prepare the ground — i18n keys, route cleanup, root layout update.

```
T1 → T2 → T3
```

### Phase 2: Tab Group Setup (Sequential)

After foundation, create the tab navigator.

```
T3 → T4
```

### Phase 3: Screen Implementation (Parallel OK)

All four screens can be created in parallel once the tab group exists and i18n keys are available.

```
         ┌→ T5 ─┐
         │      │
T4 ──────┼→ T6 ─┼── (done when all four created)
         │      │
         ├→ T7 ─┤
         │      │
         └→ T8 ─┘
```

**Note on T2 (delete old index):** The design doc identifies a HIGH risk that Metro crashes if both `app/index.tsx` and `app/(tabs)/index.tsx` exist. T2 must complete before T5 is created. T2 is placed in Phase 1 and T5 is in Phase 3, which satisfies this constraint.

---

## Task Breakdown

### T1: Add i18n keys for tab navigation labels

**What**: Add `tabs` namespace with keys `home`, `lists`, `products`, `profile`, and `comingSoon` to `i18n/locales/pt-BR.json`
**Where**: `i18n/locales/pt-BR.json` (modify)
**Depends on**: None
**Reuses**: Existing i18n JSON structure from `common`, `home`, `cart` namespaces

**Done when**:

- [ ] `tabs.home: "Início"` key exists
- [ ] `tabs.lists: "Listas"` key exists
- [ ] `tabs.products: "Produtos"` key exists
- [ ] `tabs.profile: "Perfil"` key exists
- [ ] `tabs.comingSoon: "Em breve"` key exists
- [ ] File is valid JSON

**Tests**: none
**Gate**: build

**Requirement**: NAV-01, NAV-05, NAV-07, NAV-08, NAV-09

**Commit**: `feat(i18n): add tab navigation labels in pt-BR`

---

### T2: Delete old `app/index.tsx` to avoid route conflict

**What**: Remove `app/index.tsx` to prevent Metro route conflict with the incoming `app/(tabs)/index.tsx`
**Where**: `app/index.tsx` (delete)
**Depends on**: None
**Reuses**: N/A

**Done when**:

- [ ] `app/index.tsx` no longer exists on disk
- [ ] `npm run typecheck` passes (no broken imports)

**Tests**: none
**Gate**: build

**Requirement**: NAV-01 (route conflict prevention per design risk D-07)

**Commit**: `chore(nav): remove old app/index.tsx to avoid route conflict`

---

### T3: Update root `_layout.tsx` to register `(tabs)` screen

**What**: Add `<Stack.Screen name="(tabs)" />` to the root Stack in `app/_layout.tsx` so the tab group is an explicit route
**Where**: `app/_layout.tsx` (modify)
**Depends on**: None
**Reuses**: Existing `<Stack>` component from `expo-router`

**Done when**:

- [ ] Root layout renders `<Stack><Stack.Screen name="(tabs)" /></Stack>` (explicit registration)
- [ ] `npm run lint && npm run typecheck` passes
- [ ] No visual/behavioral regression (app still loads)

**Tests**: none
**Gate**: build

**Requirement**: NAV-01 (explicit route registration per design D-01)

**Commit**: `feat(nav): register (tabs) group in root layout`

---

### T4: Create `app/(tabs)/_layout.tsx` with bottom tab configuration

**What**: Create the tab group layout with 4 `Tabs.Screen` entries, i18n titles, emoji icons, active tint color `colors.primary`, and `headerShown: false`
**Where**: `app/(tabs)/_layout.tsx` (create)
**Depends on**: T1 (i18n keys), T3 (root layout registers tabs group)
**Reuses**: `colors.primary` from `constants/colors.ts`, `useTranslation` from `react-i18next` (existing i18n pattern), `Tabs` from `expo-router`

**Done when**:

- [ ] 4 tabs are configured: Home (`/`), Lists (`/lists`), Products (`/products`), Profile (`/profile`)
- [ ] Each tab's `title` reads from i18n (e.g., `t("tabs.home")`)
- [ ] Each tab has `tabBarIcon` as a `<Text>` emoji (🏠 📋 🛒 👤)
- [ ] Active tab color is `colors.primary` (#4A90D9)
- [ ] `headerShown: false` on all screens
- [ ] Safe area handling via `useSafeAreaInsets` on parent container
- [ ] `npm run lint && npm run typecheck` passes

**Tests**: unit (render test: verify all 4 tab labels render, verify active tab color is applied)
**Gate**: quick

**Requirement**: NAV-01, NAV-02, NAV-03, NAV-04, NAV-E01, NAV-E03 (E03: Home is first/default tab)

**Commit**: `feat(nav): create bottom tab navigator with 4 tabs`

---

### T5: Create Home screen `app/(tabs)/index.tsx` [P]

**What**: Create Home screen with title "Cartflow" (i18n `home.title`), subtitle "Sua lista de compras inteligente" (i18n `home.subtitle`), button "Nova Lista" (i18n `home.newCart`), and section "Minhas Listas" (i18n `home.myCarts`)
**Where**: `app/(tabs)/index.tsx` (create)
**Depends on**: T1 (i18n keys), T4 (tabs group exists), T2 (old index deleted to avoid conflict)
**Reuses**: `useSafeAreaInsets` pattern, `Pressable` for button (per react-native-expert guidelines), `spacing` from `constants/layout.ts`

**Done when**:

- [ ] Screen renders title "Cartflow" from i18n
- [ ] Screen renders subtitle "Sua lista de compras inteligente" from i18n
- [ ] Screen renders "Nova Lista" button via `Pressable`
- [ ] Screen renders "Minhas Listas" section
- [ ] Safe area padding applied
- [ ] `npm run lint && npm run typecheck && npm run test` passes

**Tests**: unit (render test: verify title, subtitle, button, and section text appear)
**Gate**: quick

**Requirement**: NAV-05, NAV-06

**Commit**: `feat(nav): create Home screen with title, subtitle, and actions`

---

### T6: Create Lists placeholder screen `app/(tabs)/lists.tsx` [P]

**What**: Create Lists screen with centered layout showing title "Listas" (i18n `tabs.lists`) and message "Em breve" (i18n `tabs.comingSoon`)
**Where**: `app/(tabs)/lists.tsx` (create)
**Depends on**: T1 (i18n keys), T4 (tabs group exists)
**Reuses**: Placeholder pattern (same structure used by T7, T8), `useSafeAreaInsets`, `spacing`

**Done when**:

- [ ] Screen renders title "Listas" from i18n
- [ ] Screen renders message "Em breve" from i18n
- [ ] Safe area padding applied
- [ ] `npm run lint && npm run typecheck && npm run test` passes

**Tests**: unit (render test: verify title and message appear)
**Gate**: quick

**Requirement**: NAV-07

**Commit**: `feat(nav): create Lists placeholder screen`

---

### T7: Create Products placeholder screen `app/(tabs)/products.tsx` [P]

**What**: Create Products screen with centered layout showing title "Produtos" (i18n `tabs.products`) and message "Em breve" (i18n `tabs.comingSoon`)
**Where**: `app/(tabs)/products.tsx` (create)
**Depends on**: T1 (i18n keys), T4 (tabs group exists)
**Reuses**: Same placeholder pattern as T6

**Done when**:

- [ ] Screen renders title "Produtos" from i18n
- [ ] Screen renders message "Em breve" from i18n
- [ ] Safe area padding applied
- [ ] `npm run lint && npm run typecheck && npm run test` passes

**Tests**: unit (render test: verify title and message appear)
**Gate**: quick

**Requirement**: NAV-08

**Commit**: `feat(nav): create Products placeholder screen`

---

### T8: Create Profile placeholder screen `app/(tabs)/profile.tsx` [P]

**What**: Create Profile screen with centered layout showing title "Perfil" (i18n `tabs.profile`) and message "Em breve" (i18n `tabs.comingSoon`)
**Where**: `app/(tabs)/profile.tsx` (create)
**Depends on**: T1 (i18n keys), T4 (tabs group exists)
**Reuses**: Same placeholder pattern as T6

**Done when**:

- [ ] Screen renders title "Perfil" from i18n
- [ ] Screen renders message "Em breve" from i18n
- [ ] Safe area padding applied
- [ ] `npm run lint && npm run typecheck && npm run test` passes

**Tests**: unit (render test: verify title and message appear)
**Gate**: quick

**Requirement**: NAV-09

**Commit**: `feat(nav): create Profile placeholder screen`

---

## Parallel Execution Map

```
Phase 1 (Sequential):
  T1 ──→ T2 ──→ T3

Phase 2 (Sequential):
  T3 ──→ T4

Phase 3 (Parallel):
  T4 complete, then:
    ├── T5 [P]  (depends on T1, T2, T4)
    ├── T6 [P]  (depends on T1, T4)
    ├── T7 [P]  (depends on T1, T4)
    └── T8 [P]  (depends on T1, T4)
```

**[P] constraint:** Tasks marked `[P]` have no inter-task dependency within Phase 3. They can run in any order. All required test types are parallel-safe per the Parallelism Assessment.

---

## Task Granularity Check

| Task | Scope | Status |
| ---- | ----- | ------ |
| T1: Add i18n keys | 1 file modify | ✅ Granular |
| T2: Delete old index.tsx | 1 file delete | ✅ Granular |
| T3: Update root layout | 1 file modify | ✅ Granular |
| T4: Create tab layout | 1 file create | ✅ Granular |
| T5: Create Home screen | 1 file create | ✅ Granular |
| T6: Create Lists placeholder | 1 file create | ✅ Granular |
| T7: Create Products placeholder | 1 file create | ✅ Granular |
| T8: Create Profile placeholder | 1 file create | ✅ Granular |

## Diagram-Definition Cross-Check

| Task | Depends On (task body) | Diagram Shows | Status |
| ---- | ---------------------- | ------------- | ------ |
| T1 | None | No incoming arrows | ✅ Match |
| T2 | None | No incoming arrows | ✅ Match |
| T3 | None | No incoming arrows | ✅ Match |
| T4 | T1, T3 | T3 → T4 (T1 → T4 implicit) | ⚠️ Partial match — T1 dependency not shown in diagram arrow but stated in body. Acceptable: T1 key is foundational and T4 references it. |
| T5 | T1, T2, T4 | T4 → T5 | ⚠️ Partial match — T1, T2 deps not shown in diagram. Acceptable: Phase 3 parallel section text explains these deps. |
| T6 | T1, T4 | T4 → T6 | ⚠️ Same as T5 |
| T7 | T1, T4 | T4 → T7 | ⚠️ Same as T5 |
| T8 | T1, T4 | T4 → T8 | ⚠️ Same as T5 |

**Resolution:** The diagram intentionally shows only the immediate Phase 2→3 handoff (T4→T5..T8) for visual clarity. All task bodies correctly include *all* deps including cross-phase (T1 through Phase 1, T2 through Phase 1). This is an accepted trade-off for diagram readability. No structural mismatch.

## Test Co-location Validation

| Task | Code Layer Created/Modified | Matrix Requires | Task Says | Status |
| ---- | --------------------------- | --------------- | --------- | ------ |
| T1 | i18n config | none | none | ✅ OK |
| T2 | N/A (delete) | none | none | ✅ OK |
| T3 | Root layout component | unit | none | ⚠️ PROVISIONAL — see note below |
| T4 | Tab layout component | unit | unit | ✅ OK |
| T5 | Screen component | unit | unit | ✅ OK |
| T6 | Screen component | unit | unit | ✅ OK |
| T7 | Screen component | unit | unit | ✅ OK |
| T8 | Screen component | unit | unit | ✅ OK |

**Note on T3:** The coverage matrix provisionally lists root layout as requiring unit tests. However, T3 is a minor structural change (adding `<Stack.Screen name="(tabs)" />`) that does not introduce new visual content. In practice, the root layout's existing behavior (GestureHandlerRootView, SafeAreaProvider, Stack) is unchanged. The matrix row for root layout will be confirmed or adjusted during user task approval. T3 is marked `none` pending user confirmation.
